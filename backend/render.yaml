services:
  - type: web
    name: beehive-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        value: beehive-super-secret-key-2024
      - key: NODE_ENV
        value: production
